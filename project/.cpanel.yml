deployment:
  tasks:
# Next line defines a DEPLOYPATH with the path to the document root of your website so that you will not have to enter it in each command manually:
    - export DEPLOYPATH=/home/chargnnf/repositories/chamorae-ventures 
# This line copies the index.html file to deploy path - the document root of your website:
    - cp ./project/index.html $DEPLOYPATH
